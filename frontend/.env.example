# En développement, laisser vide : le proxy Vite (vite.config.ts) redirige /api
# vers http://127.0.0.1:8000 automatiquement.
# En production, définir l'URL complète de l'API Laravel déployée :
# VITE_API_URL=https://api.kaaydem.exemple.com/api/v1
VITE_API_URL=
